import "package:flutter/material.dart";

class SecurityResetPasswordUI extends StatelessWidget {
  const SecurityResetPasswordUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
