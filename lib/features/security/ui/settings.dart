import "package:flutter/material.dart";

class SecuritySettingsUI extends StatelessWidget {
  const SecuritySettingsUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
