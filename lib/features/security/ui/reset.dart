import "package:flutter/material.dart";

class SecurityResetUI extends StatelessWidget {
  const SecurityResetUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
