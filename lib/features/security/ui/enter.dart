import "package:flutter/material.dart";

class SecurityEnterUI extends StatelessWidget {
  const SecurityEnterUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
