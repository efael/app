import "package:flutter/material.dart";

class SecurityDisableUI extends StatelessWidget {
  const SecurityDisableUI({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
