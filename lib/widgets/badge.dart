import 'package:flutter/material.dart';

class BadgePin extends StatelessWidget {
  const BadgePin({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeMention extends StatelessWidget {
  const BadgeMention({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeAttach extends StatelessWidget {
  const BadgeAttach({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeAudio extends StatelessWidget {
  const BadgeAudio({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgePause extends StatelessWidget {
  const BadgePause({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeForward extends StatelessWidget {
  const BadgeForward({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeBackward extends StatelessWidget {
  const BadgeBackward({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeSkip extends StatelessWidget {
  const BadgeSkip({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgePrev extends StatelessWidget {
  const BadgePrev({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeWallet extends StatelessWidget {
  const BadgeWallet({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeLocation extends StatelessWidget {
  const BadgeLocation({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeVideoMessage extends StatelessWidget {
  const BadgeVideoMessage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeReorder extends StatelessWidget {
  const BadgeReorder({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class BadgeCount extends StatelessWidget {
  const BadgeCount({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
